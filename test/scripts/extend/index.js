'use strict';

describe('Extend', () => {
  require('./console');
  require('./filter');
  require('./generator');
  require('./helper');
  require('./migrator');
  require('./processor');
  require('./renderer');
  require('./tag');
  require('./tag_errors');
});
