'use strict';

describe('Extend', () => {
  require('./console');
  require('./filter');
  require('./generator');
  require('./helper');
  require('./processor');
  require('./renderer');
  require('./tag');
  require('./tag_errors');
});
